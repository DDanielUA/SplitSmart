package com.SplitSmart.Application;
import javax.swing.*;
import java.awt.*;

public class Main {

    public static void main(String[] args)
    {
        System.out.println("Make your friends pay!");

        BaseFrame firstFrame = new BaseFrame();

        /*
        JLabel slogan = new JLabel(); //you could just add text here
        slogan.setText("Make your friends pay!");

        JLabel name = new JLabel("SplitSmart");

        JFrame frame = new JFrame();
        frame.setTitle("SplitSmart App");
        frame.setDefaultCloseOperation((JFrame.EXIT_ON_CLOSE));
        frame.setResizable(false); //do we need this?
        frame.setSize(1024, 640);
        frame.setVisible(true);
        frame.getContentPane().setBackground(new Color(53, 190, 205));

        ImageIcon logo = new ImageIcon("logo_png.png");
        frame.setIconImage(logo.getImage()); //changes the image on the frame NOT WORKING

        frame.add(name);
        frame.add(slogan);
        */
    }
}
